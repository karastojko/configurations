[ v3_req ]
basicConstraints = CA:FALSE
keyUsage = nonRepudiation, digitalSignature, keyEncipherment
subjectAltName = @alt_names

[ alt_names ]
DNS.1 = localhost         # For local testing
DNS.2 = my.server.com     # The production hostname
IP.1 = 127.0.0.1          # If connecting via IP address
